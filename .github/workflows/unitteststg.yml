
name: unitteststg
 
on:
 push:


 

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      error: ${{ steps.error.outputs.ERROR }}

    steps:
      - uses: actions/checkout@v3
      
         

      - name: Set up Python 
        uses: actions/setup-python@v4.3.0
        with:
          python-version: 3.7
          
      - name: Install dependencies
        id: test
        run: |

            python -m pip install --upgrade pip > changed-fields.out

             output="$(cat changed-fields.out)"
             output="${output//$'\n'/\\n}"
             output="${output// / }"     # replace regular space with 'En Space'
             echo "ERROR=${ouput}" >> $GITHUB_OUTPUT
         
      - name: Persist logs
        
        if: ${{ failure() }}
        run: |
           echo "error = ${{ steps.test.outputs.ERROR}}"

  build-f:
    runs-on: ubuntu-latest
    if: failure()
    needs: [build]
    steps:
      - name: error display
        run: |
             echo "error = ${{ steps.build.outputs.error}}"
