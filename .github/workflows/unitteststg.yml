
name: unitteststg
 
on:
 push:


 

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      error: ${{ steps.error.outputs.ERROR }}

    steps:
      - uses: actions/checkout@v3
      
         

      - name: Set up Python 
        uses: actions/setup-python@v4.3.0
        with:
          python-version: 3.7
          
      - name: Install dependencies
        id: error
        run: |
            cd anu
            python -m pip install --upgrade pip > changed-fields.out
            #result_code=${PIPESTATUS[0]}
            
            #exit $result_code
            #echo $ERROR
             output="$(cat changed-fields.out)"
             output="${output//$'\n'/\\n}"
             output="${output// / }"     # replace regular space with 'En Space'
             echo "ERROR=${ouput}" >> $GITHUB_OUTPUT
         
   #   - name: Persist logs
    #    id: test
     #   if: ${{ failure() }}
      #  run: |
       #      out= "$(cat output.log)"| echo "ERROR=${out}" >> $GITHUB_OUTPUT

  build-f:
    runs-on: ubuntu-latest
    if: failure()
    needs: [build]
    steps:
      - name: error display
        run: |
             echo "error = ${{ steps.build.outputs.error}}"
