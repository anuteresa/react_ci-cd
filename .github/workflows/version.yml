on:
  push:
   tags:
      - 'v*.*'
   branches:
<<<<<<< HEAD
      - releases/v*
=======
     - main    
>>>>>>> main
  

jobs:
  version-check:
    runs-on: ubuntu-latest
   # outputs:
      #output1: ${{ steps.get_version.outputs.VERSION }}
    
    steps: 
   
       
      - uses: actions/checkout@v3
<<<<<<< HEAD
 
 
=======
        with:
          ref: main
>>>>>>> main
          
      - name: Get current package version
        
        id: package-version
        uses: martinbeentjes/npm-get-version-action@v1.3.1  
      - name: vvversion 
        run: 
          echo "version =${{ steps.package-version.outputs.current-version}}" 
<<<<<<< HEAD
=======

      - name: Generate release notes
        uses: softprops/action-gh-release@v0.1.15
        with:
          token: ${{ secrets.PAT }}
          name: production${{ steps.package-version.outputs.current-version}}
          generate_release_notes: true 
>>>>>>> main
          
      - name: Get branch name
        id: branch
        run: echo "BRANCH_NAME=${GITHUB_REF#refs/heads/}" >> $GITHUB_OUTPUT

      - name: Checkout and pull branch
        run: |
           git checkout ${{ steps.branch.outputs.BRANCH_NAME }}
 
           



     
     
