name: Nightly Deployment

on:
  workflow_dispatch:
  release:
    types: [published]
 # push:
  #  tags:
   #   - 'v*.*.*'
 
  repository_dispatch: 
      types: [tri]  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
         ref: main
    
        # Retrieves the version from package.json
      - name: Get current package version     
        id: package-version
        uses: martinbeentjes/npm-get-version-action@v1.3.1

      - name: Get next version
        uses: reecetech/version-increment@2024.4.3
        id: version
        with:
          scheme: semver
          increment: patch   

     # - name: Create Release
      #  run:  |
       #     curl -X POST \
        #    -H "Accept: application/vnd.github+json" \
         #    -H "Authorization: Bearer ${{ secrets.PAT }}" \
          #  -H "X-GitHub-Api-Version: 2022-11-28" \
           # https://api.github.com/repos/anuteresa/react_ci-cd/releases \
            # -d '{"tag_name":"v${{ steps.version.outputs.version }}","target_commitish":"development","name":"v${{ steps.version.outputs.version}}","generate_release_notes":true}' 
      - name: Debug Release Body
        run: echo "${{ github.event.release.body }}"


      - name: Set Git Identity
        run: |
      
         if echo "${{ github.event.release.body }}" | grep -q 'created_via_api: true'; then
         echo "This release was created programmatically via API."
         else
         echo "This release was published manually."
         fi
          echo '${{ steps.version.outputs.version}} '
          git config --global user.email "anuteresaj@gmail.com"
          git config --global user.name "anuteresa" 

         

          
  deploy2:
     runs-on: ubuntu-latest
     if: success() && github.event.action == 'published'
     needs: [deploy]

     steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
         ref: main      


       
    #  - name: Set API
     #   run: |
      #        curl -X POST \
       #          -H "Accept: application/vnd.github+json" \
        #         -H "Authorization: Bearer ${{ secrets.PAT }}" \
         #       -H "X-GitHub-Api-Version: 2022-11-28" \
          #      "https://api.github.com/repos/anuteresa/ci-cd/dispatches" \
           #     -d "{\"event_type\": \"trigger-workflow\"}"  
          
    


                   
          
         
    
