name: Nightly Deployment

on:
  push:
    tags:
      - 'v*.*.*'
 
  repository_dispatch: 
      types: [trigger-workflow]  
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
         ref: main
     

        # Retrieves the version from package.json
      - name: Get current package version     
        id: package-version
        uses: martinbeentjes/npm-get-version-action@v1.3.1

      - name: Set Git Identity
   
        run: |
          echo '${{ steps.version.outputs.version}} '
          git config --global user.email "anuteresaj@gmail.com"
          git config --global user.name "anuteresa" 

      - name: Download release info artifact
        uses: actions/download-artifact@v2
        with:
          name: release-info

      - name: Read release info
        run: |
          RELEASE_CREATED_BY_API=$(cat release_info.txt)
          echo "RELEASE_CREATED_BY_API: $RELEASE_CREATED_BY_API"    

          
  deploy2:
     runs-on: ubuntu-latest
     if: success() && github.event_name == 'repository_dispatch'
     needs: [deploy]

     steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
         ref: main      


       
    #  - name: Set API
     #   run: |
      #        curl -X POST \
       #          -H "Accept: application/vnd.github+json" \
        #         -H "Authorization: Bearer ${{ secrets.PAT }}" \
         #       -H "X-GitHub-Api-Version: 2022-11-28" \
          #      "https://api.github.com/repos/anuteresa/ci-cd/dispatches" \
           #     -d "{\"event_type\": \"trigger-workflow\"}"  
          
    


                   
          
         
    
